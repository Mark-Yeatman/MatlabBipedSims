params = cell2mat(flowdata.Parameters.Biped.values);
com = zeros(
for i=1:length(tout)
    com = COM_pos_func(xout(i,:), params);
end

figure('Name','Positions','NumberTitle','off')
plot(xout(:,3:dim/2),xout(:,3:dim/2))
title('CoM y phase portrait')
xlabel('\dot{y}')
ylabel('y')